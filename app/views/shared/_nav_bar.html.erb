<nav class="bg-white shadow-md fixed top-0 w-full px-6 py-4 flex justify-between items-center">
  <a href="/" class="text-2xl" font-bold text-indigo-600>Homey</a>

  <div>
    <% if user_signed_in? %>
      <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "text-white bg-red-500 px-4 py-2 rounded-lg" %>
    <% else %>
      <a href="<%= new_user_session_path %>" class="py-2 px-4 text-indigo-600">Login</a>
      <a href="<%= new_user_registration_path %>" class="ml-4 py-2 px-4 bg-indigo-600">Sign Up</a>
    <% end %>
  </div>
</nav>
